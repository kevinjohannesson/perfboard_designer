import React, { ReactElement } from 'react'

interface Props {
  
}

export default function ConnectionPoint({}: Props): ReactElement {
  return (
    <circle r="2" cx="50%" cy="50%">
      
    </circle>
  )
}
